@model Nullable<DateTime>
@{
//DateTime dt = DateTime.Now;
//if (Model != null)
//{
//    dt = (System.DateTime)Model;

//}
    @Html.TextBox("tDate", String.Format("{0:d}", Model.Value.ToString("dd.MM.yyyy")),
        new { @class = "datefield"})
}