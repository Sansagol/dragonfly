using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Dragonfly.Core
{
    /// <summary>
    /// Enum contains all cookeis types in the system.
    /// </summary>
    internal enum CookieType
    {
        UserAccessToken,
        UserId,
        UserName
    }
}